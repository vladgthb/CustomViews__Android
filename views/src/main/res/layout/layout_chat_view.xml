<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/chat_view_top_view">

    <android.support.v7.widget.RecyclerView
        android:id="@+id/chat_view_list"
        android:name="com.vladimirdanielyan.customviews.fragments.ChatView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginLeft="16dp"
        android:layout_marginRight="16dp"
        app:layoutManager="LinearLayoutManager"
        tools:context="com.vladimirdanielyan.customviews.fragments.ChatFragment"
        tools:listitem="@layout/fragment_chat" />

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_alignParentBottom="true"
        android:background="@android:color/white"
        android:id="@+id/message_area_edit_text"
        android:visibility="gone">

        <EditText
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@color/transparent"
            tools:ignore="LabelFor"
            android:inputType="text"
            android:id="@+id/new_message_edit_text_view"/>

        <ImageButton
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_marginEnd="10dp"
            android:background="@color/transparent"
            android:layout_alignParentEnd="true"
            tools:ignore="ContentDescription"
            android:id="@+id/messageSendImageButton"/>

    </RelativeLayout>

</RelativeLayout>